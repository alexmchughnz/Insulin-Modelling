My (Alex's) code doesn't directly read raw data Excel files. 

This processing was done by Klenner et al., whose code produces the .mat files "sys[x].mat".

My code loads these .mat files and does further processing and structuring to make the data nicer to work with.